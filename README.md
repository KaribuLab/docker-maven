# Imagenes de Maven con JDK

## Maven 3.5.2 with JDK 8

Validar versiones de Java y Maven:

```bash
docker run -it karibu/maven:3.5.2-jdk8 java -version
```

```bash
docker run -it karibu/maven:3.5.2-jdk8 mvn -version
```